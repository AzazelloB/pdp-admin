import {
  Box,
  Typography,
} from '@mui/material';

const HomePage: React.FC = () => {
  return (
    <Box display="flex" justifyContent="center">
      <Typography variant="h1" fontWeight="bold">Sup!</Typography>
    </Box>
  );
};

export default HomePage;
